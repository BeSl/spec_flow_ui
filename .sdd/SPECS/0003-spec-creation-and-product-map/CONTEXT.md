# Context

SpecFlow should reduce the need to manually open and edit Markdown files when creating or managing product features.

Important product decisions:

- Markdown remains the source of truth.
- New specs should be created through a guided UI wizard.
- Product features should be visualized in a dedicated map/dashboard.
- Russian localization should be supported in user-facing WebViews.
