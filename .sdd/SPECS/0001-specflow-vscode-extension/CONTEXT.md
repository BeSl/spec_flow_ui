# Context

SpecFlow for VSCode is intended for developers who work with local or extension-based LLM coding tools and want to keep SDD discipline inside the repository.

Important product decisions:

- Markdown is source of truth.
- GitHub Spec Kit compatibility is a product feature.
- Status model must stay simple.
- MVP should not require a backend or direct LLM API access.
